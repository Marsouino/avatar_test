# Project Dependencies
# ====================
# Installed in venv/ (project virtual environment).
# Policy/enforcement tools are in requirements-policy.txt (venv_policy/).

# Core
pydantic>=2.0
beartype>=0.18.0  # Runtime type validation

# Testing & type checking (run in project venv, need access to project code)
pytest>=7.0
pytest-cov>=4.0
pytest-timeout>=2.0
mypy>=1.0
