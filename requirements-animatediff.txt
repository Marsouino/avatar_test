# AnimateDiff SDXL — dépendances standalone
# Versions PyTorch alignées avec config/hardware.yaml (local: 2.10.0)
#
# GPU local : installer PyTorch avec CUDA AVANT ce fichier, sinon pip prend le build CPU
#   pip install torch==2.10.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# Puis : pip install -r requirements-animatediff.txt

torch==2.10.0
torchvision
torchaudio

diffusers>=0.27.0
transformers
accelerate
safetensors

# Config YAML
PyYAML>=6.0
